import React from 'react';
import '../../styles.css';

const Header = () => {
  return (<div className="tool">
    <div className="toolbar">Restaurant Information React App</div>
  </div>);
};

export default Header;
